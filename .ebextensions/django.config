# .ebextensions/django.config
option_settings:
  aws:elasticbeanstalk:container:python:
    # REPLACE 'health_tracker' with the name of the folder that contains settings.py
    WSGIPath: health_tracker.wsgi:application